# 5,250세션으로 60분 실행

525개 Room에 각각 10개 합성 세션을 배치해 3,600.020초를 측정했습니다. 525개 Room이 완주했고 실패 Room은 0개였습니다.

| 항목 | 기록 |
| --- | --- |
| 실행일 | 2026-08-28 |
| 워밍업 | 60초 |
| 입장 속도 | 초당 50세션 |
| 네트워크 장애 주입 | 없음 |
| Game source | `3e703cf7b0258804bdf110432db7449b9547a17c` |
| 후반 30분 RSS 기울기 | +0.14 kB/s |
| Game CPU 최대 / p95 | 0.7738 / 0.7574 |
| Room 큐 대기 표본 p99 | 5.08ms |
| 부하 생성기 스케줄러 지연 p99 | 24ms |

[공개용 측정 요약](records/capacity-5250.json)

## 측정 범위

메모리 수정이 포함된 진단본의 실행입니다. 현재 공개 루트나 전투 복구 기능을 켠 버전의 부하 측정이 아닙니다. 실제 사용자 수와 서비스 SLA로 환산하지 않습니다.

진단 코드에는 정산 요청을 즉시 Applied로 응답하는 대체 경로가 있습니다. 이번 공개 자료에는 해당 실행 이미지의 빌드 플래그를 확정할 기록이 없어 DB 정산 부하 포함 여부를 UNKNOWN으로 남겼습니다. 이 수치를 실제 MySQL 자산 반영을 포함한 전체 서비스 수용량으로 사용하지 않습니다.

Room 큐 지연은 주기적으로 수집한 latestQueueDelay 표본입니다. 모든 명령의 처리 지연 분포나 네트워크 왕복 지연이 아닙니다. 원문은 후반 30분 RSS 기울기를 kB/s로 기록했습니다. 서로 다른 구간의 기울기와 혼합하지 않습니다.

## 원본 판정과 사후 판정

60분 측정과 측정 사실 저장은 완료됐습니다. 이후 패키징이 시간 초과로 종료돼 원본 receipt는 INVALID/INCOMPLETE로 남았습니다. 저장된 측정값을 ADR-0016 기준으로 검토한 사후 판정은 PASS/COMPLETE입니다.

원본 profile의 capacityClaimEligible=false도 그대로 기록했습니다. 사후 판정이 이 필드를 바꾸거나 원본 receipt를 PASS로 덮어쓴 것은 아닙니다.

## 반복 실행에 관해

원문은 Game source 3e703cf의 두 관측 모두 실패 Room이 0이었다고 기록합니다. 이전 source 21bb4c8에서 발생한 331개 Room 실패를 동일 source의 실패로 합치지 않습니다. 두 관측만으로 반복 재현성이나 SLA를 주장하지 않습니다.

이번 공개 작업은 과거 결과의 선별 공개입니다. 새 부하를 실행하거나 측정 원본 전체를 공개한 것은 아닙니다. 운영 원문은 포함하지 않았으므로 이 요약만으로 과거 실행을 완전히 재현할 수는 없습니다.

[사례 목록](README.md)
